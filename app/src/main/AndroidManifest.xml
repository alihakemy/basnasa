<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.market">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Market"
        tools:targetApi="31">
        <activity
            android:name=".presentation.authentication.trader.traderlogin.LoginAsTrader"
            android:exported="false" />
        <activity
            android:name=".presentation.authentication.user.forget.ForgetPasswordStep1"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC2SNZwXMICS8FEfqU72VN9EkfOEEuhKyc" />

        <activity
            android:name=".presentation.location.MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".presentation.mainscreen.user.MainActivityUser"
            android:exported="false" />
        <activity
            android:name=".presentation.authentication.user.forget.ForgetPasswordStep3"
            android:exported="false" />
        <activity
            android:name=".presentation.authentication.user.forget.ForgetPasswordStep2"
            android:exported="false" />
        <activity
            android:name=".presentation.authentication.user.create.CreateUserAccountActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.authentication.user.login.LoginUser"
            android:exported="true" />
        <activity
            android:name=".presentation.onboarding.OnBoarding"
            android:exported="false" />
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/splashScreenTheme">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
    </application>

</manifest>